description = "Create an isolated git worktree."
prompt = "Activate the code-forge:worktree skill. Please process the following input or context: {{args}}. If no input is provided, proceed with the default workflow for worktree."
