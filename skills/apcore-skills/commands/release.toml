description = "Coordinated multi-repo release orchestration and pipeline."
argument-hint = "<version>"
prompt = "Activating apcore-skills:release. Please orchestrate the multi-repo release for version: {{args}}"
